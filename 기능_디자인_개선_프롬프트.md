# 기능/편의성 및 디자인 개선 요청 (Claude Code용 프롬프트)

## 배경
배경 조사(코드 기반)가 진행되는 동안, 직접 실행 중인 사이트(localhost:8888)에 로그인해서 알림/마이페이지/모바일 화면을 직접 눌러보며 확인했다. 확인된 내용은 다음과 같다.

- 예시로 들었던 "차단 취소" 기능은 실제로 이미 구현되어 있음. /mypage/activity?tab=blocks에 "차단 목록" 탭 + "차단 해제" 버튼이 구현돼 있음(UserBlockController.java:30). 즉 이건 "이런 느낌으로 찾아달라"는 예시였던 것으로 이해하고, 다른 실제로 빠진 기능들을 계속 찾음.
- 알림 클릭 시 원본 글로 이동이 안 되는 것을 실제로 확인함 — /notifications 페이지 DOM을 확인해보니 알림 한 줄 한 줄이 전부 `<a href>` 링크가 아니라 그냥 "읽음 처리"만 하는 `<button>`임. "admin님이 좋아요 눌렀어요" 알림을 클릭해도 그 글로 이동하지 않음.
- 아이디/비밀번호 찾기 기능이 아예 없음 — AuthController에 로그인/회원가입/마이페이지 관련 엔드포인트는 다 있는데 찾기/재설정 관련은 하나도 없음.
- 디자인 쪽: theme.css에 브랜드 컬러 시스템은 꽤 잘 잡혀있음(그라데이션, danger 톤 맞춤, reduced-motion 대응까지). 하지만 다크모드가 전혀 없고, 삭제/신고 등 확인창은 커스텀 모달이 아니라 브라우저 기본 confirm()/alert()를 32번이나 그대로 씀 — 공들인 디자인 톤과 안 맞는 조잡한 팝업이 여기저기서 튀어나옴.

이후 코드 기반 감사도 종료되어, 직접 사이트를 눌러본 내용과 합쳐 아래와 같이 최종 정리했다.

## 작업 지시
아래 정리된 기능/편의성 문제와 디자인 문제를 검토하고 개선해줘.

---

## 🔧 기능/편의성 문제

### 1. 신고를 취소할 방법이 없음 (3곳 전부)
게시글(PostController.java)/댓글/한마디 전부 report()만 있고 취소(cancelReport) 메서드는 어디에도 없다. "내 활동내역"에도 "내가 신고한 글 목록" 탭 자체가 없어서, 잘못 눌렀거나 마음이 바뀌어도 되돌릴 방법도, 심지어 내가 뭘 신고했었는지 다시 확인할 방법도 없다. 처음 예시로 든 "차단 취소 없음"과 정확히 같은 종류의 문제인데, 차단은 이미 고쳐져 있고 신고는 안 고쳐진 상태다.

### 2. 알림이 클릭해도 이동 안 되고, 지울 수도 없음
직접 확인한 것과 에이전트가 찾은 것을 합치면: 알림 한 줄이 `<a href>`가 아니라 그냥 "읽음 처리" `<button>`이라 클릭해도 해당 글/댓글로 이동하지 않는다. 게다가 "모두 읽음 처리"만 있고 개별 삭제가 없어서 알림함이 계속 쌓이기만 한다.

### 3. 아이디/비밀번호 찾기가 아예 없음
로컬 계정으로 가입했는데 비밀번호를 잊으면 자력으로는 복구할 방법이 없다(로그인 상태에서만 정보 수정 가능). 실제 서비스라면 꽤 치명적인 누락이다.

### 4. 공지사항을 올린 후 수정/삭제가 안 됨
NoticeService엔 작성 관련 메서드만 있고 수정/삭제가 없다. 오타 하나 나도 못 고치고, 고치려면 새 공지를 다시 올려야 하는데 그럼 전체 유저에게 알림이 또 간다.

### 5. 프로필 페이지에서 바로 차단이 안 됨
차단 버튼이 게시글/댓글 안에 인라인으로만 있어서, 그 사람 글을 찾아 들어가야만 차단할 수 있다. /users/{id} 프로필 화면엔 차단 버튼이 없다.

### 6. 관리자 신고 목록 - 기간 필터 없음, 처리 이력 없음
키워드 검색만 되고 "이번 주 신고만 보기" 같은 날짜 필터가 없다. 더 크게는, 어떤 관리자가 언제 블라인드/해제 처리했는지 기록(감사 로그)이 아예 없다 — 지금 상태(블라인드 여부)만 보이고 "누가 그렇게 만들었는지" 히스토리가 없다.

### 7. 게시글 검색에 "작성자로 검색"이 없음
제목/내용 검색만 있고 닉네임으로 찾는 옵션이 없다.

### 8. 일괄 처리(체크박스로 여러 개 선택) 기능이 전무함
관리자 화면이든 내 활동내역이든, 신고/삭제/읽음 처리 전부 한 번에 하나씩만 가능하다.

### 9. 이미지 업로드 용량 제한(5MB)을 서버만 검사함
글을 다 쓰고 제출 버튼을 눌러야만 "파일이 너무 큽니다" 에러를 알게 된다 — 미리 알려주는 클라이언트 체크가 없어서 타이핑한 내용을 날릴 위험이 있다.

### 10. 글쓰기 중 뒤로가기/실수로 페이지 이탈해도 경고가 없음, 임시저장 기능도 없음

### 11. 마이페이지에서 소개글(bio) 수정 시 미리보기가 없음
저장 전엔 실제 프로필에 어떻게 보일지 알 수 없다.

---

## 🎨 디자인 문제

### 다크모드가 전혀 없음
theme.css에 브랜드 컬러 시스템은 잘 짜여있는데 prefers-color-scheme 대응이 없다.

### 삭제/신고 확인창이 브라우저 기본 confirm()/alert()
커스텀 디자인 시스템을 32곳에서 쓰다가 정작 확인/경고 팝업만 브라우저 기본 흰 박스로 튀어나와서 디자인 톤이 깨진다.

### mypage.css/auth.css/notice.css/school-search.css에는 반응형(@media) 규칙이 하나도 없음
다만 실제로 모바일 크기(375px)에서 열어봤을 땐 안 깨졌다(플렉스박스 덕에 우연히 버티는 걸로 보임). 의도적으로 설계된 반응형은 아니라는 게 문제다.

---

## 정정 사항
처음에 준 예시(차단 취소)와, 혹시 있을까 싶었던 "학교/학년/반 나중에 변경"은 둘 다 실제로 이미 구현돼 있었다(각각 UserBlockController.java:30, /mypage/edit). 확인 없이 "이것도 없을 것 같다"고 넘겨짚지 않고 코드로 직접 검증한 것이니 참고할 것.

## 다음 단계
위 항목들 중 우선순위를 정해서 어떤 것부터 살펴볼지 정리하고, 각 항목에 대한 개선 방안을 제시해줘.
