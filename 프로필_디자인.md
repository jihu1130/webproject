# 프로필 화면 리디자인 설계도

세 프로필 화면 — **마이페이지**(`/mypage`, 본인용) · **공개 프로필**(`/users/{id}`, 남이 보는) ·
**관리자용 프로필**(`/admin/profiles/{id}`) — 을 하나의 설계로 다시 짠다.
**이 문서는 계획만 정리한 것으로, 실제 코드는 건드리지 않았다.**

색상·타이포는 기존에 만든 [웹스쿨 디자인 시스템 — 생활기록부 리디자인](https://claude.ai/code/artifact/7ff275a2-1e10-4ccc-b659-28ec6a39e242) 아티팩트의 토큰(`--m-*`, Noto Serif KR + IBM Plex Sans KR, "스탬프" 배지)을 그대로 이어받는다 — 새 화면 하나마다 새 디자인 언어를 또 만들지 않기 위함.

---

## 1. 지금 뭐가 문제인가 (코드로 확인한 사실)

세 화면이 **서로 다른 컴포넌트를 억지로 재사용**하고 있어서, 같은 "프로필"인데 완전히 다른 화면처럼 보인다.

| 화면 | 실제 재사용 중인 컴포넌트 | 문제 |
|---|---|---|
| 마이페이지 (`mypage.html`) | 전용 `.mypage-card` | 그나마 가장 프로필다움. 그러나 아바타가 "닉네임 첫 글자 원형"뿐이고, 활동 통계(글/댓글/좋아요 수)가 전혀 없음 |
| 공개 프로필 (`profile.html`) | **게시글 상세 카드**(`.post-detail-card`, `post.css`)를 그대로 재사용 | 아바타·역할 배지·학교 정보·통계 **전부 없음**. `<h1>` 아이콘 + 닉네임 + 소개글 + 차단 버튼이 끝. 마치 "게시글 하나"처럼 보임(실제로 게시글 상세와 시각적으로 구분이 안 됨) |
| 관리자용 프로필 (`admin/user-profile.html`) | **관리자 목록/로그 카드**(`.admin-detail-card`, `admin.css`)를 그대로 재사용 | 아바타 없음. 상태·역할이 감사 로그와 똑같은 `.admin-status-badge` 알약 배지라서 "이 계정의 정체성"이 아니라 "로그 항목"처럼 읽힘. 정보가 전부 `<dl>` 표 형태라 스캔하기 어려움 |

공통 문제:
1. **아바타 처리가 화면마다 다르거나 아예 없다** — 마이페이지만 있고, 나머지 둘은 없음.
2. **활동 통계가 관리자 화면에만 있고, 그마저도 게시글만 링크되고 댓글은 목록 자체가 없음**(수정사항.md #17). 정작 본인·타인이 보는 화면엔 통계가 아예 없어서 "이 사람이 활동을 많이 하는 사람인지" 알 길이 없다.
3. **셋 다 "신원 카드"라는 공통 개념이 없어서** 관리자가 계정 관리하다 공개 프로필을 봐도, 유저가 마이페이지 보다 남의 프로필을 봐도 완전히 다른 화면처럼 느껴짐 — 같은 사이트라는 일관성이 없다.

---

## 2. 설계 원칙

### 2-1. "프로필 카드"를 하나의 공유 컴포넌트로 만든다
세 화면 모두 **같은 뼈대**(아바타 + 이름/아이디 + 배지 + 통계 바 + 정보 그리드)를 쓰고, **컨텍스트에 따라 보이는 액션·정보만 다르게** 한다. 완전히 다른 컴포넌트 3개가 아니라, "누가 보느냐"에 따라 슬롯이 켜지고 꺼지는 **하나의 컴포넌트 + variant**로 취급한다.

| | 마이페이지 (본인) | 공개 프로필 (타인) | 관리자 프로필 |
|---|---|---|---|
| 아바타 | ✅ | ✅ | ✅ |
| 이름/아이디 | ✅ | ✅ (닉네임만, 아이디는 비공개) | ✅ (아이디 포함) |
| 역할 배지 | ✅ | ❌ (일반 유저 사이엔 굳이 노출 안 함 — 학생 사회에서 "부관리자" 배지가 또래 사이 위계처럼 보이는 걸 방지) | ✅ |
| 학교/학년/반 | ✅ | ❌ (개인정보 — 지금도 공개 프로필엔 없음, 유지) | ✅ |
| 활동 통계(글·댓글·좋아요) | ✅ 본인 것 | ✅ **공개 가능한 것만**(글 수만, 신고/제재 내역 제외) | ✅ 전체 |
| 소개글 | ✅ 수정 가능 | ✅ 읽기 전용 | ➖ (관리자 화면엔 불필요, 생략) |
| 최근 게시글 | ➖ (마이페이지엔 "내 활동내역"으로 이미 분리돼 있어 중복 생략) | ✅ | ✅ |
| 최근 댓글 | ➖ | ➖ (공개 프로필엔 지금도 없고, 필요성도 낮음 — 유지) | ✅ **신규**(수정사항.md #17 해결) |
| 차단 버튼 | ➖ | ✅ | ➖ (관리자는 "제재"라는 더 강한 도구가 이미 있음) |
| 제재 부여/이력 | ➖ | ➖ | ✅ |
| 감사 로그 링크 | ➖ | ➖ | ✅ **신규**(수정사항.md #11과 연결 — 이 프로필에서 바로 "이 계정에 대한 감사 로그 보기"로 갈 수 있게) |

### 2-2. 아바타: 이니셜 원형은 유지하되, "신원 카드"답게 격을 높인다
지금 마이페이지의 이니셜 원형(`--brand-gradient` 배경 + 흰 글자)은 나쁘지 않은 기본기 — 그대로 컴포넌트화해서 3곳에 재사용한다. 다만:
- 역할에 따라 테두리 색을 다르게 해서(총관리자=골드, 부관리자=브랜드블루, 학생=중립 톤) 한눈에 "이 사람이 뭔지" 파악되게 한다.
- 구글 로그인 계정은 아바타 우측 하단에 작은 구글 아이콘 배지를 얹는다(지금은 "구글 계정" 텍스트 배지가 따로 붙어 있어 시각적으로 번잡함 — 아바타 안으로 흡수).

### 2-3. 배지는 "신원용"과 "로그/상태용"을 시각적으로 분리한다
지금 관리자 프로필은 계정 상태(활동중/탈퇴함)도, 역할(총관리자/부관리자)도 전부 감사 로그와 똑같은 알약 배지(`.admin-status-badge`)를 쓴다. 새 설계에서는:
- **역할 배지**: 아바타 테두리 색 + 이름 옆 작은 "스탬프"(생활기록부 테마의 `.m-stamp`) — 명사형("총관리자")
- **상태 배지**: 이름 아래 별도 줄, 다른 모양(사각 태그) — 동사형 상태("활동중" / "비활성화됨" / "탈퇴함")
이렇게 위치·모양을 분리하면 "이 사람이 누구인지"와 "지금 상태가 어떤지"가 한눈에 구분된다.

### 2-4. 통계는 숫자만 나열하지 않고, 눌러서 실제 목록으로 가는 진입점으로 만든다
지금 관리자 프로필의 "작성 게시글/작성 댓글" 박스는 그냥 숫자다(클릭 안 됨). 새 설계에서 통계 칸 자체를 링크로 만든다 — "게시글 9" 클릭 시 아래 "최근 게시글" 섹션으로 스크롤, 또는 관리자 화면에서는 `/admin/posts?keyword=작성자닉네임`처럼 필터된 목록으로 이동.

---

## 3. 공통 컴포넌트 스펙 (`.profile-card`)

```
┌─────────────────────────────────────────────┐
│  [아바타]   닉네임              [스탬프: 역할]  │
│   원형      @아이디                            │
│   72px      [상태 태그: 활동중]                 │
│                                               │
│  ─────────────────────────────────────────  │
│  [통계] 9        [통계] 2        [통계] 14     │
│   게시글          댓글            받은 좋아요    │
│  ─────────────────────────────────────────  │
│                                               │
│  관심 학교   아산배방중학교                      │
│  학년 / 반   3학년 1반                          │
│  (관리자 화면에서만) 관리자 권한  O O X X         │
│                                               │
│  ┌ 소개글 ──────────────────────────────┐    │
│  │ ...                          [✏️ 수정] │    │
│  └───────────────────────────────────────┘    │
│                                               │
│  [본인: 정보 수정] [본인: 활동내역] [타인: 차단]  │
└─────────────────────────────────────────────┘
```

- 컨테이너: 기존 `.post-detail-card`/`.admin-detail-card`와 동일한 라운드(18px)·그림자 톤을 계승하되, 상단에 **얇은 브랜드 그라디언트 바**(지금 `.post-detail-card::before`에만 있던 디테일)를 세 화면 다 통일해서 적용 — "이건 프로필 카드"라는 시각적 시그니처로 삼는다.
- 아바타 72px, 역할별 3px 테두리(총관리자 `--m-accent` 골드 / 부관리자 `--m-brand` 블루 / 학생 `--m-line` 중립).
- 통계 바: `.m-stat` 컴포넌트를 그대로 재사용(이미 아티팩트에 정의됨 — 카드형, `tabular-nums`로 숫자 정렬).
- 정보 그리드: 기존 `.admin-profile-grid`(2열 dl) 그대로 재사용, 라벨/값 순서 유지.
- 소개글 박스: 기존 `.mypage-profile-box` 그대로 재사용(본인 화면만 수정 아이콘 노출).

---

## 4. 화면별 상세

### 4-1. 마이페이지 (`/mypage`)
- 지금 구조를 대부분 유지하되, **소개글 박스 위에 통계 바를 추가**한다(글/댓글/좋아요 수 — 이미 `/mypage/activity`에서 세는 값들이라 서버 쪽 신규 쿼리 불필요, 개수만 상단에 요약).
- "구글 계정" 텍스트 배지를 없애고 아바타에 구글 배지로 흡수(2-2 참고).
- 역할 배지를 아바타 테두리 색 + 이름 옆 스탬프로 교체, 기존 `.mypage-role-badge`(원형 알약)는 폐기.

### 4-2. 공개 프로필 (`/users/{id}`)
가장 시급 — 지금 유일하게 "프로필 전용 카드"가 아예 없는 화면.
- `.post-detail-card` 재사용을 그만두고 신규 `.profile-card`를 이 화면에 처음 적용.
- 아바타 + 닉네임 + 소개글까지는 지금과 같은 정보량, 여기에 **"작성 게시글 N" 통계 하나만 추가**(신고/제재 등 민감 정보는 여전히 비공개 — 2-1 표 참고).
- 차단 버튼은 지금처럼 유지하되, 카드 우측 상단으로 위치 이동(지금은 제목 바로 아래 좌측이라 "메인 액션"처럼 눈에 띔 — 차단은 자주 쓰는 기능이 아니므로 톤을 낮춤).
- 게시글 목록은 지금 그대로(카드 아래 리스트) 유지 — 이 부분은 문제 없었음.

### 4-3. 관리자용 프로필 (`/admin/profiles/{id}`)
- 상태·역할 배지를 2-3 방식으로 분리.
- 통계 바에 "게시글"·"댓글" 두 칸 다음에 **"최근 댓글" 섹션을 게시글과 동일한 패턴으로 신규 추가**(수정사항.md #17) — `.admin-report-item` 카드 재사용, `/admin/comments/{id}`로 링크.
- 카드 하단에 **"감사 로그에서 이 관리자 조치 보기"** 링크 한 줄 추가(role이 ADMIN/SUPER_ADMIN일 때만) — `/admin/audit-log?adminUsername={username}`로 연결(수정사항.md #11 audit-log 링크 작업과 세트로 처리하면 자연스러움).
- 제재 부여 폼·제재 이력은 지금 위치(카드 아래 별도 섹션) 유지 — 이건 "프로필"이 아니라 "조치"의 영역이라 카드 안에 넣지 않고 분리해둔 지금 구조가 맞다.

---

## 5. 반응형 (375px 기준)
- 통계 바 3칸은 데스크톱 가로 배치, 375px에서는 `.admin-profile-stats`가 이미 쓰는 패턴대로 `flex-wrap`으로 자동 줄바꿈(2+1 또는 1열).
- 정보 그리드(`.admin-profile-grid`)는 이미 560px 이하에서 1열로 바뀌는 규칙이 있음 — 그대로 재사용.
- 아바타 72px → 모바일에서 56px로 축소, 이름과 나란히가 아니라 위아래 중앙 정렬(마이페이지가 이미 이 레이아웃).

## 6. 다크모드
아티팩트에 이미 정의된 `--m-*` 다크 팔레트를 그대로 따른다 — 아바타 테두리 색(골드/블루)은 라이트·다크 모두 채도를 낮추지 않고 유지(신원 표시는 테마와 무관하게 항상 눈에 띄어야 함).

---

## 7. 적용 우선순위

1. ~~**공개 프로필(`/users/{id}`)에 `.profile-card` 신규 적용**~~ — **완료.** `profile-card.css` 신설, 아바타+통계(작성 게시글 수, `posts.getTotalElements()`로 신규 쿼리 없이 계산)를 `.post-detail-card` 안에 추가, 차단 버튼은 카드 우측 상단으로 이동.
2. ~~**관리자 프로필에 "최근 댓글" 섹션 추가**~~ — **완료.** (이 항목은 다른 세션이 수정사항.md #17을 처리하며 먼저 구현해뒀음 — `AdminUserProfileDto.recentComments` + 템플릿 섹션. 이번 라운드에서는 미리보기 2줄 클램프 스타일만 추가로 입혔다.)
3. ~~**배지 체계 분리(역할 vs 상태)**~~ — **완료.** 세 화면 전부 `.profile-card-stamp`(역할, 둥근 알약)와 `.profile-card-status`(상태, 사각 태그)로 분리 적용.
4. ~~**마이페이지 통계 바 추가, 아바타 구글 배지 흡수**~~ — **완료.** `MyActivityService.getStats()` 신규(게시글/댓글 수는 기존 카운트 재사용, 좋아요는 `PostRepository.sumLikeCountByAuthor_Id` 신규 집계 쿼리 추가). 구글 뱃지는 아바타 우하단 아이콘으로 흡수, 텍스트 배지 삭제.
5. ~~**감사 로그 상호 링크(관리자 프로필 ↔ 감사 로그)**~~ — **완료.** 관리자 프로필 카드에 "감사 로그에서 이 계정의 조치 내역 보기" 링크 추가(`/admin/audit-log?adminUsername={username}`). 반대 방향(감사 로그 → 프로필)은 이미 다른 세션이 수정사항.md #11에서 처리해둠.

**적용 후 발견해서 고친 버그 2개** (브라우저로 모바일 375px + 다크모드까지 직접 재검증하며 발견):
- 공개 프로필의 차단 버튼이 데스크톱은 `position: absolute`라 카드 우측 상단에 잘 붙지만, 모바일에서 `position: static`으로 바뀌는 순간 원래 DOM 순서(아바타보다 앞)가 그대로 드러나 아바타 위에 붕 뜬 것처럼 보였다 — 버튼을 카드 맨 끝(소개글 아래)으로 옮겨서 해결.
- 아바타 배경을 `--brand-gradient`(=`--brand-1`)로 뒀는데, 이 토큰은 다크모드에서 "어두운 배경 위 강조색"으로 쓰라고 밝은 하늘색(`#7ea6e6`)으로 뒤집힌다 — 흰 글자와 대비가 2.48:1까지 떨어져 WCAG 최소 기준(큰 텍스트 3:1)도 못 채웠다. `.admin-profile-stat`이 이미 쓰는 "라이트/다크 모드 상관없이 항상 짙은 남색 고정"인 `--nav-bg`로 교체해서 18.3:1(다크)/15.7:1(라이트)로 해결. 나머지 스탬프·상태 태그·통계 텍스트는 전부 실측 결과 4.5:1 이상(5.0~13.7:1) 통과 확인.

**실제 적용 시 설계와 달라진 점**: 이 문서 3장의 시안은 아티팩트([웹스쿨 디자인 시스템](https://claude.ai/code/artifact/7ff275a2-1e10-4ccc-b659-28ec6a39e242))의 `--m-*` 토큰·Noto Serif KR을 참조했지만, 실제 코드에는 지금 사이트가 이미 쓰고 있는 `theme.css`의 `--brand-1`/`--accent`/`--success`/`--warn`/`--danger`/`--surface-soft` 등 실제 라이브 토큰만 사용했다(같은 "생활기록부" 색 체계를 이미 `theme.css`가 반영하고 있어서 값 자체는 사실상 동일 — 다만 별도 서체·`--m-` 접두사 토큰을 새로 끌어오면 사이트 나머지 40여 화면과 디자인 언어가 갈라지므로, 지금 라이브 토큰 그대로 재사용). `.profile-card` 전용 컨테이너도 새로 만들지 않고 기존 `.mypage-card`/`.post-detail-card`/`.admin-detail-card`를 그대로 쓰고 내부 조각(아바타·배지·통계)만 신규 클래스로 교체했다 — 컨테이너까지 새로 만드는 건 이번 범위에서 불필요한 중복이라 판단.
