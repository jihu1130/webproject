# 코드 감사 결과 버그 수정 요청 (Claude Code용 프롬프트)

## 배경
세 개의 독립적인 감사 에이전트를 통해 프로젝트의 DB 연결/데이터 처리, 캘린더, 관리자 기능을 각각 감사했다. 감사는 다음 순서로 진행되었다.
- DB 연결/데이터 처리 감사 완료 (1/3)
- 캘린더 감사 완료 (2/3)
- 관리자 감사 완료 (3/3)

세 감사 모두 완료되었으며, 흥미로운 점은 같은 버그를 3개 에이전트가 전부 독립적으로 찾아냈다는 것이다 — 그만큼 확실한 버그라는 뜻이다. 아래는 심각도 순으로 정리한 결과이다.

## 작업 지시
아래 발견된 버그들을 확인하고 수정해줘. 각 버그의 위치, 원인, 재현 방법, 참고할 기존 수정 패턴을 참고해서 고쳐줘.

---

## 🔴 최우선 — 3개 감사 모두 동일하게 지목한 버그

### ScheduleCommentService.java:234-241 — 총관리자가 블라인드된 "오늘의 한마디" 원본을 못 봄

```java
private boolean isAdmin(String username) {
    return userRepository.findByUsername(username)
            .map(u -> u.getRole() == User.Role.ROLE_ADMIN)  // ROLE_SUPER_ADMIN 누락
            .orElse(false);
}
```

- 문제: role == 'ROLE_ADMIN'처럼 딱 하나의 role 값만 비교하면 ROLE_SUPER_ADMIN은 빠진다.
- 참고: PostService.isAdmin() / PostCommentService.isAdmin()은 이미 이 버그를 겪고 User.isAdmin()(= ROLE_ADMIN || ROLE_SUPER_ADMIN)으로 고쳐놨음 (코드 주석에 "버그 수정"이라고 명시되어 있음). ScheduleCommentService만 고쳐진 적이 없는 상태.
- 재현 방법: admin/admin(총관리자) 계정으로 로그인해서 신고 3번 누적된 한마디를 캘린더 화면에서 보면, 실제 내용 대신 "블라인드 처리된 한마디입니다" 안내문만 뜬다 (관리자 페이지로 따로 가야만 원본을 볼 수 있음).
- 수정 방향: 이미 코드에 있는 패턴(User.isAdmin()) 그대로 적용.

---

## 🟠 캘린더/학사일정

### SchoolService.java:127-134 — 방학 D-Day가 "방학식" 하루만 등록한 학교에서 방학 내내 틀림
코드 자체 주석에 "학교마다 방학 전체 기간을 등록하기도 하고 방학식/개학 하루만 등록하기도 한다"고 써있는데, getVacationDday()는 "오늘 날짜에 '방학'이라는 이름의 일정이 있는가"만 확인한다. 방학식 당일만 통과하고, 그다음 날부터는 실제로 방학인데도 "방학 아님"으로 처리되어 D-Day가 사라지거나 심하면 다음 학기 방학까지의 D-100+로 잘못 표시될 수 있다. 이 기능의 핵심 목적(방학 D-Day)이 이 방식을 쓰는 학교에서는 거의 통째로 안 맞는 셈이다.

### SchoolService.java:169-206 — 방학 마지막 날에 종료일 계산 실패
"개학" 이벤트가 따로 없고 "방학 태그된 마지막 날 다음 날 = 개학일"로 추론하는 로직인데, 정작 방학 마지막 날 당일에는 이 추론이 실행 안 되고 null(정보 없음)을 반환한다. 하필 사용자가 제일 궁금할 그날 뜨는 게 "정보 없음"인 아이러니한 버그.

### NeisApiService.java:195-225 — 기숙사 학교 급식이 조식을 중식으로 표시
급식 종류(조식/중식/석식) 필터 없이 응답에서 제일 처음 나오는 메뉴를 그냥 가져다 쓴다. 기숙사가 있어서 조식/중식/석식을 다 등록하는 학교는 조식이 "오늘의 급식(중식)"으로 잘못 표시된다.

### SchoolService.java:74-88 — 시간표 캐시에 UNIQUE 제약이 없어서 동시 요청 시 같은 교시가 중복 저장
같은 반 학생 두 명이 아직 캐시 안 된 날짜를 거의 동시에 열람하면 둘 다 캐시 미스 → 둘 다 NEIS 조회 → 둘 다 DB에 저장, 결과적으로 "1교시"가 두 번 뜬다. 신고 unique 제약이랑 같은 종류 문제인데, 여긴 그 제약 자체가 없는 케이스.

### calendar.js:683-687 — escapeHtml()이 따옴표(")를 이스케이프 안 해서 댓글 수정창에서 self-XSS 가능
div.textContent → div.innerHTML 방식은 <, >, &는 막아도 "는 못 막아서, 댓글 내용에 "가 들어있으면 value="..." 속성이 깨지면서 임의 스크립트 삽입이 가능하다. 지금은 본인 댓글 수정 화면에서만 쓰여서 "자기 자신 공격"에 그치지만, 이스케이프 함수 자체가 불완전한 건 실제 버그다.

### ScheduleCommentService.java:262-288 — 탈퇴한 사용자 닉네임이 "탈퇴한 사용자"로 안 바뀜
프로필 링크는 막는데(authorLinkable=false) 닉네임 텍스트 자체는 실제 닉네임 그대로 노출된다 — CLAUDE.md에 적힌 "탈퇴 계정 닉네임 치환" 원칙이 이 화면만 빠져있다.

---

## 🟡 관리자 기능

### NoticeService.java:51-58 — "활성 공지 항상 1개" 규칙이 동시성에 취약
관리자 두 명이 거의 동시에 공지를 새로 작성하면 둘 다 "활성 공지 1개" 상태를 동시에 읽어버려서, 결과적으로 활성 공지가 2개 생길 수 있다. 그러면 그다음 findByActiveTrue()(1개만 기대하는 조회)가 예외를 던져서 공지 배너 자체가 500 에러로 깨진다.

### AdminHomeController.java:18-34 — 공지사항 권한만 가진 부관리자가 /admin 접속 시 "접근 거부" 화면으로 감
신고/게시글/한마디 권한만 체크하고 canManageNotices는 안 본다. 실제 /admin/notices는 문제없이 들어갈 수 있는데 진입점 라우팅만 빠뜨린 것 — 나중에 공지 기능 추가하면서 이 부분만 업데이트를 놓친 전형적인 케이스.

### AdminScheduleCommentService.java — 한마디 신고 처리 시 작성자에게 알림이 안 감
같은 성격의 AdminPostService(게시글/댓글)는 블라인드·신고해제 시 작성자에게 알림을 보내는데, 한마디 쪽만 그 로직이 통째로 빠져있다 (NotificationService 의존성 자체가 없음).

---

## 🟢 DB/데이터 처리

### PostService.java 등 — likeCount/viewCount/reportCount가 동시 요청에 취약(lost update)
신고는 unique 제약이라는 안전장치가 있지만, 좋아요 수/조회수는 그냥 count+1을 읽고 다시 저장하는 방식이라 두 요청이 거의 동시에 들어오면 하나의 +1이 사라질 수 있다. race condition과 같은 종류인데, 여긴 방어 장치가 아예 없는 버전이다.

### PostCommentService.java:51-56 — 댓글 목록 조회 시 N+1 쿼리 폭탄
댓글 C개짜리 글 하나를 열람할 때마다 최대 1 + 4C번의 SQL이 실행된다(작성자 조회 + 신고/좋아요/북마크 여부 확인이 댓글마다 따로 실행). 이게 사이트에서 제일 트래픽 많은 화면(글 상세)이라 댓글이 많아질수록 느려진다.

### PostRepository.search() — 게시글 목록도 같은 패턴, size=100까지 요청 가능해서 최악의 경우 101번 쿼리
(참고, 낮은 우선순위) 관리자 목록 화면들도 같은 N+1 패턴이지만 데이터 규모가 작다는 전제라 영향이 적다.

---

## 요약 및 우선순위
- #1(총관리자 role 체크 누락)은 100% 확실하고 고치기도 제일 쉬움.
- #7(공지 동시성)과 #2(방학 D-Day)는 이 사이트의 핵심 기능이 실제로 틀리게 동작하는 케이스라 우선순위 높게 볼만함.
- 위 버그 중 우선순위가 높은 것부터 순서대로 검토하고 수정 방안을 제시해줘.
