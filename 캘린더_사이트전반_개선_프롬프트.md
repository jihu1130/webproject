# 캘린더/한마디 UX 및 사이트 전반 개선 요청 (Claude Code용 프롬프트)

## 배경
두 가지 방향으로 감사를 진행했다: 캘린더/한마디 UX, 그리고 사이트 전체(내비게이션·접근성·에러 메시지·관리자 UX). 진행 순서는 다음과 같다.
- 캘린더/한마디 감사 완료 (1/2)
- 사이트 전체 감사 완료 (2/2)

두 감사 모두 완료되었으며, 아래는 이번 라운드에서 새로 나온 내용만 정리한 것이다(이전에 이미 정리된 내용은 반복하지 않음).

## 작업 지시
아래 정리된 캘린더/한마디 UX 문제와 사이트 전반 문제를 검토하고 개선해줘.

---

## 📅 캘린더 / 오늘의 한마디

### 1. 한마디에는 알림이 아예 안 감 (가장 크게 체감될 문제)
ScheduleCommentService엔 NotificationService가 아예 안 붙어있다. 커뮤니티 댓글/좋아요는 알림이 가는데(PostCommentService), 누가 내 한마디에 좋아요를 누르거나 댓글을 달아도 평생 알 방법이 없다 — 매일 그 날짜 페이지를 직접 다시 열어봐야만 알 수 있다. 가장 자주 쓰는 기능일 텐데 제일 크게 빠진 부분이다.

### 2. 차단한 사람이 한마디에서는 그대로 보임 (차단이 반쪽짜리)
게시글 댓글은 작성 전에 userBlockService.assertNotBlocked(...)로 차단 여부를 확인하는데, 한마디는 이 체크 자체가 없다. 누군가를 차단해도 같은 반 "오늘의 한마디" 스레드에서는 여전히 마주치고, 그 사람도 여전히 댓글을 달 수 있다.

### 3. "학교를 못 찾겠어요" 안내가 어디에도 없음
회원가입/마이페이지수정/학교설정 전부 같은 학교 검색창을 쓰는데, 검색 결과가 0개면 그냥 "일치하는 학교가 없습니다"만 뜨고 끝이다 — 문의처, 대안 경로가 아예 없다. 신설 학교거나 이름이 조금 다르게 등록된 학생은 여기서 완전히 막힌다(가입 자체가 안 되니까 더 치명적).

### 4. 반 목록이 없을 때 가짜 목록(1반~20반)을 진짜처럼 보여줌
NEIS에 학년별 반 정보가 없으면 class-select.js가 조용히 "1반"~"20반"이라는 임의 목록으로 대체해버린다. 사용자는 이게 진짜 데이터인지 가짜인지 구분할 방법이 없어서, 실제로 존재하지 않는 반을 골라버릴 수 있다.

### 5. 급식 알레르기 정보를 서버가 일부러 지워버림
NEIS 원본 데이터엔 "돈까스(5.6.10)"처럼 알레르기 코드가 붙어오는데, NeisApiService가 정규식으로 그 괄호 부분을 파싱 과정에서 통째로 제거하고 있다(NeisApiService.java:217 근처). 단순히 "안 보여주는" 게 아니라 "서버가 갖고 있던 정보를 일부러 버리는" 것이라, 알레르기 있는 학생/부모 입장에서는 꽤 아쉬운 부분이다.

### 6. 시간표 로딩 중엔 그냥 빈 화면
급식/한마디는 "불러오는 중..." 표시가 있는데 시간표만 없다.

### 7. 날짜 상세 패널에 "이전 날/다음 날" 버튼이 없음
닫고 다른 칸을 다시 클릭해야 한다.

### 8. 시간표 인쇄/이미지 저장/공유 기능이 아예 없음
화면 안에서만 볼 수 있다.

---

## 🧭 사이트 전반 (내비게이션 / 에러 처리 / 접근성)

### 9. 뭔가 실패해도 아무 설명 없이 그냥 튕겨나감 — 제일 자주 겪을 문제
PostController의 글 상세/수정/삭제 여러 곳에서 잘못된 접근이 일어나면 에러 메시지 없이 그냥 다른 페이지로 조용히 리다이렉트만 된다(PostController.java:99 등). 오래된 링크를 눌렀거나 이미 삭제된 글에 접근하면 "왜 안 되는지" 전혀 모른 채 목록으로 튕겨나간다. 관리자 화면은 더 심해서, AdminUserController의 승격/강등/삭제/정지 액션이 실패해도 성공했는지 실패했는지조차 티가 안 난다 — 화면이 그냥 그대로 새로고침된다.

### 10. 글 쓰던 중 로그인이 만료되면 내용이 전부 조용히 날아감
경고도, 임시저장도 없이 로그인 페이지로 튕기고, 다시 로그인하면 그냥 커뮤니티 목록으로 간다 — 쓰던 글은 흔적도 없이 사라진다.

### 11. 통합 검색이 없고, 공지사항은 검색 자체가 없음
커뮤니티/캘린더/공지가 각각 따로국밥이라, "기말고사"를 찾으려면 세 군데를 따로 찾아봐야 한다. 공지사항은 그마저도 검색창 자체가 없어서 스크롤로 다 훑어야 한다.

### 12. 모바일 햄버거 메뉴가 없고, 관리자 하위 화면엔 breadcrumb(현재 위치 표시)이 없음
모바일에서 메뉴가 접히는 게 아니라 그냥 줄바꿈만 돼서 늘어난다. 관리자 화면 깊숙이 들어가면(예: 계정 관리 > 특정 유저 프로필) "지금 내가 어디 있는지" 표시가 전혀 없어서 뒤로가기에만 의존해야 한다.

### 13. 접근성 — 아이콘 전용 버튼에 제대로 된 라벨이 없고, 사이트 전체에 포커스 표시(:focus-visible)가 없음
좋아요/북마크/신고/차단 버튼들이 아이콘 하나에 title 속성만 있고 aria-label이 없어서 스크린리더 사용자는 그냥 "버튼"으로만 듣는다. 키보드로 Tab 이동할 때도 사이트 전체에 뚜렷한 포커스 링이 없어서(입력창만 살짝 테두리 색이 바뀜) 지금 어디에 포커스가 있는지 눈으로 구분하기 힘들다.

### 14. 관리자 권한 토글이 확인창도, 저장됐다는 피드백도 없이 클릭 즉시 반영됨
체크박스 하나 잘못 눌러도 바로 부관리자 권한이 바뀌어버리고, "저장됨" 같은 표시도 없어서 실수로 눌렀는지도 모르고 지나칠 수 있다.

### 15. 회원가입 완료 후 자동 로그인이 안 되고, 비밀번호 강도 표시도 없음
학교/학년/반까지 다 입력하고 가입 완료했는데 바로 로그인 페이지로 보내져서 아이디/비번을 또 입력해야 한다.

## 16. 개시물이나 한마디에 대하여 숨기기(비공개 일부만 공개(링크로 재한적 공개))기능을 구현

---

## 참고 — 확인해보니 문제 없던 것들 (괜히 의심 안 하도록)
- 캘린더 이벤트 검색은 실제로 해당 월로 잘 이동하고, "오늘로" 버튼도 이미 있음.
- 회원가입 비밀번호 확인은 실시간으로 이미 체크됨.
- 관리자 계정 목록엔 이미 검색/필터가 있음.
- 게시글 이미지엔 이미 alt 텍스트가 붙어있음.
- "티어/랭킹 준비중"은 클릭 안 되게 잘 처리돼있음.

## 다음 단계
이걸로 4개 라운드 감사(초기 3개 + 후속 2개)가 모두 합쳐졌다. 위 항목들을 하나로 정리하거나, 우선순위를 정해서 어떤 것부터 개선할지 제시해줘.
