# 수정사항 — 편의성 · 디자인 개선 제안

## 조사 방법
- 로컬에서 이미 떠 있던 서버(`localhost:8888`)에 **Chrome 브라우저(Claude in Browser)로 직접 접속**해서 일반회원(`user1`/`user1`, 부관리자 권한 보유)과 총관리자(`admin`/`admin`)로 각각 로그인해 화면을 하나씩 클릭·조작하며 확인.
- DOM/접근성 트리, 콘솔 로그, computed style(다크모드 미디어쿼리, `:focus-visible` 등)을 코드로 직접 조회해서 "눈으로 봐서 괜찮아 보이는 것"이 아니라 실제 마크업/동작 기준으로 확인.
- 브라우저에서 확인한 내용은 실제 백엔드/프론트 소스코드와 대조해서 근거(파일:라인)를 남김.
- **이 문서는 목록 정리만 한 것으로, 코드는 전혀 수정하지 않았음.**
- 기존에 `기능_디자인_개선_프롬프트.md` / `캘린더_사이트전반_개선_프롬프트.md` 등에 있던 항목 중 상당수가 이미 고쳐져 있는 걸 실제로 확인함 — 아래 "이미 해결된 항목"에 정리해뒀으니 재작업하지 않도록 주의.

---

## ✅ 참고 — 이전 문서에 있었지만 이번에 확인해보니 이미 해결된 항목
다시 손대지 않도록 먼저 정리한다.

| 항목 | 확인 내용 |
|---|---|
| 삭제/신고 확인창이 브라우저 기본 `confirm()`/`alert()` | `static/js/modal.js`의 `WebSchoolModal`로 전면 교체 완료. 코드 전체에서 `modal.js` 밖의 raw `confirm()`/`alert()` 호출 0건 확인 |
| 신고 취소 불가 / "내가 신고한 글" 목록 없음 | `MyActivityService`/`Controller`에 신고 관련 로직 존재, `/mypage/activity?tab=reports`에 게시글·댓글·한마디 하위 탭까지 확인됨 |
| 공지사항 수정/삭제 불가 | `NoticeService.updateNotice()` / `deleteNotice()` 존재 확인 |
| 프로필 페이지에서 바로 차단 불가 | `user/profile.html`에 차단 버튼 + `WebSchoolModal.confirm` 확인 |
| 관리자 신고 목록 — 기간 필터/처리 이력 없음 | `/admin/reports`에 시작일·종료일 필터, 사이드 메뉴에 **"8. 감사 로그"**(`/admin/audit-log`) 별도 메뉴로 존재 |
| 게시글 검색에 작성자 검색 없음 | `/posts` 검색 옵션에 "작성자" 라디오 버튼 존재 |
| 관리자 권한 토글에 확인/피드백 없음 | `admin-permissions.html`에서 `WebSchoolModal.confirm('권한 설정을 변경할까요?')` 확인 |
| 아이콘 전용 버튼에 라벨 없음 | 댓글 좋아요/북마크/채택/수정/삭제 버튼 전부 `aria-label` + `title` 동시 보유 확인 |
| 사이트 전체 포커스 표시(`:focus-visible`) 없음 | `theme.css`에 전역 `:focus-visible` 규칙 확인 |
| 모바일 햄버거 메뉴 없음 | 375px 폭에서 "메뉴 열기" 버튼(`#siteNavToggle`, `aria-expanded`/`aria-controls` 포함) 확인, 클릭 시 정상 동작 |
| 글쓰기 중 이탈 시 경고 없음 | `post-form.js`에 `beforeunload` + dirty 감지 로직 존재(단, 임시저장까지는 아님 — 아래 항목 참고) |
| 시간표 인쇄/이전날·다음날 버튼 없음 | 캘린더 화면에 "시간표 인쇄", "전날"/"다음날" 버튼 모두 확인됨 |
| 반 목록이 없을 때 가짜 목록을 진짜처럼 보여줌 | "NEIS에 반 정보가 없어 1~20반을 표시합니다(실제와 다를 수 있음)" 안내 문구 확인됨 |

---

## 1. 백엔드

### 1-1. 데이터베이스(DB) 관련
1. **시간표 캐시 테이블에 UNIQUE 제약이 없음** (`school/domain/Timetable.java`, `SchoolService.java:74-88`)
   같은 반 학생 여러 명이 캐시 안 된 날짜를 동시에 열람하면 각자 NEIS 조회 → 각자 저장이 일어나 같은 교시가 중복 저장될 수 있음. 신고 기능은 "유니크 제약"으로 막아뒀는데 여긴 그 안전장치가 없는 케이스.
   → 개선 방향: `(school_code, grade, class_no, date, period)` 조합에 `@UniqueConstraint` 추가.

2. **좋아요 수/조회수/신고 수가 단순 read-then-write 방식** (`PostService.java` 좋아요 처리부, 264~278번 줄 부근)
   `post.setLikeCount(post.getLikeCount() + 1)` 형태로 값을 읽고 다시 저장하는 방식이라, 거의 동시에 여러 요청이 들어오면 하나의 +1이 사라지는 lost update가 발생할 수 있음(신고 카운트처럼 유니크 제약으로 막혀있지 않음).
   → 개선 방향: `UPDATE post SET like_count = like_count + 1 WHERE id = ?` 형태의 원자적 증감 쿼리(`@Modifying` + JPQL)로 교체, 또는 낙관적 락(`@Version`).

3. **댓글 목록 조회 시 N+1 쿼리** (`PostCommentService.java:51-56`)
   댓글이 C개면 최대 `1 + 4C`번 쿼리가 실행됨(작성자/신고여부/좋아요여부/북마크여부를 댓글마다 개별 조회). 트래픽이 제일 많은 글 상세 화면이라 댓글 많은 글일수록 체감 지연이 커짐.
   → 개선 방향: `fetch join` 또는 댓글 id 목록으로 한 번에 배치 조회 후 메모리에서 매핑.

4. **게시글 목록 조회도 같은 N+1 패턴** (`PostRepository.search()`) — size=100까지 요청 가능해 최악의 경우 101번 쿼리. 우선순위는 3번보다 낮음(관리자 목록 위주라 데이터 규모 작음).

5. **공지사항 "활성 공지는 항상 1개" 처리가 원자적이지 않음** (`NoticeService.java:createNotice`, 44~60번 줄 부근)
   `findByActiveTrueAndDeletedFalse()`로 기존 활성 공지를 찾아 비활성화한 뒤 새 공지를 활성으로 저장하는 두 단계 로직. 관리자 두 명이 거의 동시에 공지를 작성하면 활성 공지가 2개가 될 수 있고, 이후 "활성 공지 1개"를 전제로 하는 조회(`findByActiveTrue`류)가 깨질 위험이 있음.
   → 개선 방향: DB 유니크 인덱스(`active=true`인 행은 항상 1개, 부분 인덱스 또는 애플리케이션 락)나 비관적 락으로 원자성 보장.

### 1-2. 백엔드 로직/API (DB 외)
6. **NEIS 급식 응답에서 알레르기 정보를 서버가 통째로 제거함** (`NeisApiService.java:239` 부근)
   `"돈까스(5.6.10)"`처럼 원본에 붙어오는 알레르기 유발물질 코드를 정규식으로 파싱 단계에서 삭제. "안 보여준다"가 아니라 "서버가 갖고 있던 정보를 저장 전에 버린다"는 점이 문제 — 알레르기 있는 학생/학부모 입장에서 실질적으로 아쉬운 데이터 손실.
   → 개선 방향: 원본 알레르기 코드를 별도 필드로 저장해두고, 화면에 "(알레르기: 5.돼지고기, 6.우유...)" 형태로 노출하거나 최소한 원문을 보존.

7. **오늘의 한마디(ScheduleComment)에 알림 연동이 전혀 없음** (`ScheduleCommentService.java` — `NotificationService` 의존성 자체가 없음)
   커뮤니티 댓글/좋아요는 알림이 가는데(`PostCommentService`), 한마디는 누가 좋아요를 누르거나 답글을 달아도 평생 알 방법이 없음. 매일 같은 날짜 페이지를 다시 열어봐야만 확인 가능. 캘린더/한마디가 실사용 빈도가 높은 기능일 가능성이 큰 만큼 체감 영향이 크다.
   → 개선 방향: `PostCommentService`의 알림 발송 패턴을 그대로 `ScheduleCommentService`에 이식.

8. **오늘의 한마디에는 차단 여부 검증이 없음** (`ScheduleCommentService.java` — `assertNotBlocked` 호출 없음, `PostCommentService.java:79`엔 있음)
   게시글 댓글은 작성 전 `userBlockService.assertNotBlocked(...)`로 차단 여부를 확인하는데 한마디는 이 검사 자체가 빠져 있어, 차단한 사람의 글이 같은 반 한마디 스레드에서는 그대로 보이고 그 사람도 여전히 댓글을 달 수 있음 — 차단 기능이 절반만 동작.
   → 개선 방향: `ScheduleCommentService`의 작성 로직에 동일한 `assertNotBlocked` 호출 추가.

9. **이미지 업로드 용량(5MB) 검사가 서버에만 있음** — 클라이언트 검증 없음 (`static/js/post-form.js` 56~80번 줄, 이미지 미리보기 로직에 파일 크기 체크 없음 확인)
   글을 다 쓰고 제출해야만 "파일이 너무 큽니다" 에러를 알 수 있음. `beforeunload` 이탈 경고는 이미 있어서 완전히 날아가진 않지만, 미리 알려주면 애초에 실수를 막을 수 있음.
   → 개선 방향: `imageInput`의 `change` 이벤트에서 `file.size` 체크 후 즉시 안내.

10. **글쓰기 임시저장(draft) 기능 없음** — `post-form.js` 주석에 "임시저장까지는 이번 범위 밖"이라고 명시되어 있어, 이탈 경고만 있고 실제 복구 수단은 없음. 로그인 만료로 튕겨나가는 경우엔 경고창도 못 뜨고 그대로 날아감.
    → 개선 방향: `localStorage`에 제목/본문 주기적 자동 저장 → 글쓰기 화면 재진입 시 "이어서 작성하시겠어요?" 복구 제안.

---

## 2. 프론트엔드

### 2-1. 일반 사용자(User) 화면
11. **로그인 화면에 아이디/비밀번호 찾기가 없음** — `/login` 페이지 실제 확인, 링크 자체가 없음. 로컬 계정으로 비밀번호를 잊으면 자력 복구 불가(로그인 상태에서만 정보 수정 가능).

12. **알림을 클릭해도 원본 글/댓글로 이동하지 않음** — `/notifications` 실제 확인 결과, 알림 한 줄이 여전히 `<a href>`가 아니라 `/notifications/{id}/read`로 POST하는 `<button>`. "user2님이 오늘의 한마디를 좋아합니다" 같은 알림을 눌러도 그 글로 이동하지 않고 읽음 처리만 됨. (개별 삭제 버튼은 이미 추가되어 있음 — 여기는 이미 해결됨)
    → 개선 방향: 알림에 대상 URL을 함께 저장해두고, 클릭 시 읽음 처리 + 해당 위치로 이동을 한 번에 처리(예: JS로 POST 후 리다이렉트, 또는 아예 `<a>`로 감싸고 읽음 처리는 별도 아이콘 버튼으로 분리).

13. **공지사항 목록에 검색 기능이 없음** — `/notices` 실제 확인, 검색창 자체가 없어 스크롤로 다 훑어야 함.

14. **커뮤니티/캘린더/공지를 가로지르는 통합 검색이 없음** — 세 영역이 각자 따로 검색을 제공해서, 예를 들어 "기말고사"를 찾으려면 세 군데를 각각 검색해야 함.

15. **시간표 로딩 중 표시가 없음** — 캘린더 상세 패널에서 급식/한마디는 로딩 상태가 있는데 시간표만 없어서, 로딩 중엔 그냥 빈 화면처럼 보임.

16. **다크모드 CSS 커버리지가 일부 화면에만 적용됨** — `prefers-color-scheme: dark` 규칙이 `theme.css`/`post.css`/`calendar.css`/`school-search.css`/`mypage.css`/`index.css`/`auth.css` 7개 파일엔 있지만, **`navbar.css`, `notification.css`, `notice.css` 3개 파일엔 없음**. 즉 다크모드를 켜면 본문 카드/캘린더 등은 어둡게 바뀌는데 상단 네비게이션 바·알림함·공지사항 화면만 밝은 배경으로 남아 화면 톤이 어긋남.
    → 개선 방향: 세 파일에 나머지 파일들과 동일한 CSS 변수(`var(--surface)`, `var(--ink)` 등) 기반 다크모드 규칙 추가.

17. **마이페이지 소개글(bio) 수정 화면에 미리보기 없음** (`/mypage/profile` 실제 확인) — 텍스트 입력창과 저장 버튼만 있고, 실제 공개 프로필(`/users/{id}`)에서 어떻게 보일지 저장 전엔 알 수 없음.

18. **부관리자 계정이 권한 없이도 "관리자" 메뉴를 그대로 봄** (아래 2-2 12번과 연결되는 사용자 관점 이슈) — 아무 관리 권한이 없는 계정도 상단에 "관리자" 링크가 노출되어 눌러보게 만들고, 결국 "접근 권한 없음" 화면만 마주하게 됨. 일반 사용자 입장에선 혼란스러운 막다른 길.

### 2-2. 관리자(부관리자) 화면
19. **"관리자" 네비게이션 링크가 실제 권한과 무관하게 항상 노출됨** — `user1`(부관리자, 4개 권한 모두 미부여 상태)로 로그인 후 실제로 "관리자" 링크를 클릭해 확인함. `/admin`으로 이동은 되지만 즉시 "아직 이 메뉴에 대한 권한이 없어요"라는 접근 거부 화면만 뜸.
    → 개선 방향: 권한이 하나라도 있을 때만 링크를 노출하거나, 최소한 접근 거부 화면에 "권한 요청하기" 같은 다음 행동을 안내.

20. **관리자 목록 화면 전체에 일괄 처리(체크박스 다중 선택) 기능이 없음** — `/admin/posts`, `/admin/comments`, `/admin/schedule-comments`, `/admin/users` 등 실제 확인, 체크박스 자체가 없어 신고 처리/삭제/정지 등을 전부 한 번에 하나씩만 처리 가능.

21. **관리자 화면 깊은 경로에 breadcrumb(현재 위치) 없음** — 예: 계정 관리 → 특정 유저 프로필(`/admin/profiles/9`)로 들어가면 왼쪽 "1.신고 관리 ~ 8.감사 로그" 사이드 메뉴만 있고 "관리자 > 계정 관리 > user1" 같은 위치 표시가 없어 뒤로가기에만 의존해야 함.

22. **`admin.css`에 다크모드(`prefers-color-scheme`) 대응이 없음** — 위 16번과 같은 종류 문제인데 관리자 화면 전용. 관리자 화면은 표/사이드 메뉴가 많아 명암 대비가 안 맞으면 특히 눈에 띔.

### 2-3. 최고관리자(Super Admin) 전용 화면
23. **부관리자 권한을 "전부 꺼진" 상태로 방치해도 목록에서 구분이 안 됨** (`/admin/users/admins` 실제 확인) — `user1`처럼 4개 권한이 모두 꺼진 부관리자도 화면상 다른 부관리자와 동일하게 표시됨. 총관리자가 "이 계정은 사실상 아무 권한도 없다"를 목록에서 한눈에 알아채기 어려움.
    → 개선 방향: 권한이 0개인 부관리자 행에 경고 배지("권한 없음")를 표시.

24. 계정 관리(`/admin/users`)·관리자 권한(`/admin/users/admins`)은 총관리자만 접근 가능한 화면인데, 위 20번(일괄 처리 없음)·21번(breadcrumb 없음)이 이 화면들에도 동일하게 해당됨 — 총관리자가 계정이 많아질수록 가장 크게 체감할 문제.

---

## 3. 모바일 환경(375px 기준) 불편사항
- ✅ **사이트 상단 내비게이션은 햄버거 메뉴로 이미 잘 접힘** — "메뉴 열기" 버튼(`aria-expanded`/`aria-controls` 포함, 접근성 속성까지 정상) 확인, 문제 없음.
- ✅ 커뮤니티/게시글 목록 등 주요 화면은 가로 스크롤(overflow-x) 없이 375px에서 정상 표시됨.
- ✅ 관리자 표(table)는 `.admin-table-wrap`에 `overflow-x: auto`가 걸려 있어 화면이 깨지지 않고 가로 스크롤로 처리됨 — 레이아웃이 부서지진 않음.
- **25. 관리자 화면 전용 사이드 메뉴("1. 신고 관리" ~ "8. 감사 로그")가 모바일에서 접히지 않음** — 사이트 전체 내비게이션과 달리 이 메뉴는 별도 컴포넌트라서, 모바일에서 관리자 페이지에 들어갈 때마다 8줄짜리 메뉴 목록이 화면 위쪽을 그대로 차지하고, 실제 표/콘텐츠를 보려면 매번 스크롤을 내려야 함.
  → 개선 방향: 상단 내비게이션과 같은 접이식(아코디언/드롭다운) 패턴 적용.
- **26. 관리자 표는 가로 스크롤은 되지만 모바일 전용 카드형 레이아웃은 아님** — 상태/카테고리/제목/작성자/신고자 수/작성일 등 열이 많은 표를 좌우로 계속 스크롤해야 전체 정보를 볼 수 있음. 총관리자·부관리자가 외부에서 스마트폰으로 처리할 상황을 고려하면 불편함이 큼.
- **27. 다크모드 커버리지 문제(16, 22번)는 모바일에서 더 두드러짐** — 시스템이 다크모드인 스마트폰에서 접속하면 네비게이션 바/알림함/공지 화면이 밝은 배경인 채로 다른 화면과 섞여 뜬다.
- 16번(공지사항 검색 없음)·14번(통합검색 없음)도 모바일에서는 스크롤 탐색 비용이 데스크톱보다 훨씬 크므로 모바일 관점에서도 우선순위 높게 볼 만함.

---

## 우선순위 제안 (참고용)

**체감 영향이 큰 것부터**
1. 한마디 알림 미연동(#7) — 가장 자주 쓸 기능인데 알림이 아예 없음
2. 한마디에서 차단이 반쪽짜리(#8) — 차단 기능 자체의 신뢰가 깨지는 문제
3. 알림 클릭 시 이동 안 됨(#12) — 알림함을 열어도 실제로 쓸모가 없어짐
4. 아이디/비밀번호 찾기 없음(#11) — 로컬 계정 사용자에게 치명적 이탈 요인
5. 관리자 링크가 권한 없이도 노출(#19) — 신규 부관리자가 매번 겪을 혼란

**적은 노력 대비 효과가 큰 것**
- 이미지 업로드 클라이언트 사이드 용량 체크(#9)
- 다크모드 CSS 3개 파일 보강(#16, #22, #27)
- 공지사항 검색창 추가(#13)
- 부관리자 "권한 없음" 배지(#23)

**구조적 작업이 필요한 것(우선순위는 낮지만 데이터 정합성 문제)**
- 좋아요/조회수 lost update(#2), 시간표 캐시 UNIQUE 제약(#1), 공지 활성화 동시성(#5), 댓글 N+1(#3)
