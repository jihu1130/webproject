# 수정사항 — 기능 오류 · 불편사항 (4차 정리)

## 조사 방법
- `git log` + `git status`로 이전 라운드 이후 반영된 변경사항을 먼저 확인(직전 커밋 `e12d8b3`, 이후 로그인 버튼 클릭 불가 버그 수정이 미커밋 상태로 추가 반영됨).
- `localhost:8888`에 Chrome 브라우저로 직접 접속해 일반회원(`user1`)·총관리자(`admin`)로 로그인 후 재검증, 소스코드 대조.
- **4차 라운드**: "진짜 그거밖에 없냐"는 지적을 받고, 이번엔 글쓰기/댓글/좋아요/북마크/신고/차단/QnA 채택/캘린더 일정검색/통합검색/마이페이지/관리자 계정·권한·공지 화면까지 실제로 하나하나 클릭하며 재검사. 로그인 상태로 `/login`·`/register` 직접 접근, 자문자답 채택, `admin-search-form`이 페이지 전체를 모바일에서 가로로 밀어내는 것까지 컴퓨티드 스타일로 재확인해 아래 신규 항목(#15~#18)으로 추가.
- **이 문서 자체는 목록 정리만 한 것으로, 4차 조사 시점엔 코드를 수정하지 않았음.** 이후 별도 세션에서
  이 문서를 읽고 #2(오판으로 정정), #10·#11·#14~#18(코드 수정 + 브라우저 재검증 후 해결 확인·삭제)을
  처리했다 — 각 항목이 지워진 자리에 남은 인용구(`>`)가 그 처리 기록. #12·#13(권한 위임/총관리자
  승계 구조)은 사용자 확정으로 이번 라운드 범위 밖.
- 지시에 따라 **이미 해결된 항목은 표로 남기지 않고 정리해서 지웠다** — 해결 여부는 이번 조사에서 실제로 재확인한 것만 반영.
- 디자인 관련 항목은 이 문서에 텍스트로 나열하는 대신, **완전히 새로운 방향의 디자인 시스템 설계도**를 별도 아티팩트로 만들었다 → [웹스쿨 디자인 시스템 — 생활기록부 리디자인](https://claude.ai/code/artifact/7ff275a2-1e10-4ccc-b659-28ec6a39e242). 색상·타이포·컴포넌트 토큰과 관리자/로그인/커뮤니티 3개 화면 목업(라이트·다크 각각 전환 가능)이 들어있고, 아래에서 발견한 대비 부족·배지 불일치 문제를 전부 그 설계 안에서 직접 해결하는 형태로 반영했다.

---

## 🐛 기능적 오류 (버그)

> ~~**관리자 일괄 처리 폼 CSRF 토큰 누락**~~ — 이번 라운드에 코드 대조 + 실제로 "선택 블라인드" 버튼을 눌러 재확인함: `SecurityConfig`의 `csrf.disable()`이 제거되고 `static/js/csrf.js` 헬퍼가 추가돼 `admin-bulk.js` 등 JS로 동적 생성하는 폼에도 토큰이 정상적으로 실려서 403 없이 처리됨. **해결 확인, 삭제.**

> ~~**알림을 클릭해도 원본 글/댓글로 이동하지 않음**~~ — 코드를 직접 대조한 결과 **오판이었다**: `Notification.link` 필드가 알림 생성 시점에 이미 실제 URL(`/posts/{uuid}` 등)로 채워지고 있고(`PostService`/`PostCommentService`), `NotificationController.read()`가 그 링크로 정상 리다이렉트한다(코드 확인 완료). DOM이 `<a href>`가 아니라 `<button>`(form 안)인 것만 보고 "이동 안 됨"으로 오판한 것으로 보인다. **코드 변경 없음, 오판으로 기록.**

> ~~**로그인/회원가입 상태인데도 `/login`, `/register`에 그대로 들어가면 폼이 다시 뜸**~~ — `AuthController.loginPage()`/`registerPage()`에 인증 확인 가드를 추가해 이미 로그인된 사용자가 두 경로에 접근하면 `/`로 리다이렉트하도록 수정. 로그인 상태에서 `/login`·`/register` 접근 시 홈으로 튕기는 것 브라우저로 확인. **해결 확인, 삭제.**

> ~~**QnA 질문자가 본인 질문에 스스로 답을 달고 채택 가능**~~ — `PostCommentService.acceptAnswer()`에 "댓글 작성자 == 요청자"면 막는 서버 검증 추가 + `post-detail.js`의 채택 버튼 노출 조건에 `!c.mine` 추가로 프론트에서도 숨김. 본인 댓글에 버튼이 안 보이는 것과, 직접 API 호출 시 400(`본인이 작성한 답변은 채택할 수 없습니다.`)으로 막히는 것까지 확인. **해결 확인, 삭제.**

> ~~**상대 프로필(`/users/{id}`)에서 차단이 안 됨**~~ — 다른 세션이 프로필_디자인.md 작업으로 이 화면에 차단 버튼을 추가하면서, `post-detail.js`의 `blockUser()`와 동일한 요청을 인라인 스크립트로 복제했는데 CSRF 재활성화(이 문서 위 CSRF 항목) 당시엔 아직 존재하지 않던 페이지라 `<meta name="_csrf">`/`csrf.js` 로드가 빠진 채로 남아있었다 — CSRF가 켜진 뒤로 이 페이지의 `POST /users/blocks`만 조용히 403으로 실패하고 있었음. 두 `<meta>` 태그 + `csrf.js` 스크립트 추가, `fetch()` 헤더에 `WebSchoolCsrf.headers()` 추가로 해결. `admin`으로 `user1` 차단 → 200 응답 + "차단했습니다" 메시지 확인 → 마이페이지 차단 목록에서 해제까지 확인. **해결 확인, 삭제.**

### 프론트엔드 — 관리자 (신규 발견, 스크린샷 기반 재점검)

> ~~**감사 로그 검색창(`.admin-search-form`)에 select/date 스타일 없음 + 모바일 페이지 전체 가로스크롤**~~ — `.admin-search-form`에 `flex-wrap: wrap` 추가(필드가 넘치면 다음 줄로 내려감) + `select`/`input[type="date"]`에 텍스트 입력창과 동일한 톤의 스타일 규칙 추가(다크모드 대응 포함). 375px 뷰포트에서 `document.body.scrollWidth`가 375(뷰포트와 동일, 이전엔 603)로 줄어든 것과 select/date 테두리가 둥글게 나오는 것까지 확인. **해결 확인, 삭제.**

> ~~**감사 로그의 "관리자"·"대상" 칸이 링크 없이 텍스트뿐**~~ — `AdminActionLogDto`에 `adminUserId`/`targetUrl` 추가, `AdminActionLogService`가 targetType(POST/COMMENT/SCHEDULE_COMMENT/USER)별로 이미 있는 관리자 상세 라우트를 계산해서 채워준다(COMMENT는 부모 게시글 id를 조회해 `/admin/posts/{postId}#comment-{id}` 앵커로). 4가지 targetType 전부 실제 링크가 걸리는 것(`/admin/profiles/1`, `/admin/posts/22`, `/admin/posts/20#comment-6`, `/school/comments/9`, `/admin/users/10/profile`)을 브라우저로 확인. **해결 확인, 삭제.**

> ~~**관리자 프로필 화면에 "작성 댓글" 개수만 있고 목록이 없음**~~ — "최근 작성 게시글"과 동일한 패턴으로 "최근 작성 댓글" 섹션 추가(`PostCommentRepository.findTop5ByAuthor_Id...` 신규, `AdminUserProfileCommentDto` 신규). 클릭하면 부모 게시글 상세의 해당 댓글 위치(`#comment-{id}`)로 이동하는 것까지 확인. **해결 확인, 삭제.**

> ~~**탈퇴한 계정이 "관리자 권한 관리" 화면에 여전히 노출**~~ — `AdminUserService.getAllAdmins()`에 `!u.isDeleted()` 필터 추가. 실제로 탈퇴 처리된 `brandtest01`(ROLE_ADMIN, deleted=1)이 `/admin/users/admins` 목록에서 빠진 것을 DB 대조 + 브라우저로 확인. **해결 확인, 삭제.**

---

## 😕 불편한 점 (기능은 되지만 개선이 필요한 것)

### 백엔드

> ~~**이메일 기반 아이디/비밀번호 찾기가 구조적으로 불가능**~~ — `User.email` 필드 추가 + `EmailToken`/`EmailTokenService`/`MailService`로 인증·재설정 인프라 전체 구현(`/find-username`, `/forgot-password`, `/reset-password`, `/verify-email`). 실사용자 Gmail 계정의 앱 비밀번호로 SMTP를 붙여 실제 메일 수신까지 확인, 토큰 발급/소비/재사용 차단, 새 비밀번호로 실로그인까지 브라우저로 end-to-end 검증. 기존 계정은 `EmailSetupInterceptor`가 로그인 시 이메일 등록을 강제한다. **해결 확인, 삭제.**

> ~~**게시글 목록 검색(`PostRepository.search()`)이 N+1 패턴**~~ — `search()`에 `@EntityGraph(attributePaths = "author")` 추가(`PostCommentRepository`가 이미 쓰던 것과 동일 패턴, `author`가 `@ManyToOne`이라 `@EntityGraph`+`Pageable` 조합의 "인메모리 페이징" 문제와 무관). 커뮤니티 목록/작성자 검색 둘 다 회귀 없이 동작하는 것 확인. **해결 확인, 삭제.**

> ~~**리치 에디터 "저장 안 된 임시 업로드" 파일 정리 안 됨**~~ — 이 프로젝트 첫 `@Scheduled` 작업(`EditorUploadCleanupService`, 매일 새벽 4시)으로 해결. Post/한마디 본문(소프트 삭제 포함)에서 실제 참조되는 파일 URL을 모아두고, 디스크에서 그 목록에 없으면서 24시간 이상 지난 파일만 삭제(방금 올렸지만 저장 전인 파일 보호용 유예). 참조된 파일/24시간 이내 파일/24시간 지난 미참조 파일 3가지 케이스를 실제로 만들어 서비스 메서드를 직접 호출해 검증 — 의도한 파일 1개만 정확히 삭제되는 것 확인. **해결 확인, 삭제.**

### 프론트엔드 — 일반 사용자

> ~~**아이디/비밀번호 찾기 링크가 로그인 화면에 없음**~~ — 3번과 함께 해결. `login.html`에 "아이디 찾기 · 비밀번호 찾기" 링크 추가. **해결 확인, 삭제.**

### 프론트엔드 — 관리자

> ~~**계정 관리(`/admin/users`) 화면에만 일괄 처리 체크박스가 없음**~~ — 게시글/댓글/한마디 관리와 동일한 `admin-bulk.js` 패턴 재사용(선택 비활성화/선택 탈퇴 2개, 기존 행별 버튼과 동일 범위). `AdminUserController`에 `bulk-deactivate`/`bulk-delete` 신규(기존 단건 메서드 그대로 재사용, 본인/총관리자 보호는 그 메서드들에 이미 있음). 체크박스 2개 선택 → 일괄 비활성화 → 둘 다 반영되는 것 확인. **해결 확인, 삭제.**

> ~~**모바일 관리자 탭바에 "더 있다"는 스크롤 힌트 없음**~~ — 새 엘리먼트 추가 없이 `.admin-main-tabs`에 `mask-image`로 오른쪽 끝이 항상 옅게 페이드되도록 처리(스크롤 가능하다는 시각적 신호, JS 불필요). 375px에서 마스크 적용 확인. **해결 확인, 삭제.**

### 프론트엔드 — 최고관리자

> ~~7번(계정 관리 일괄 처리 없음)은 총관리자 화면에도 동일 적용됨~~ — 위 7번 항목과 함께 해결. **해결 확인, 삭제.**

---

## 🔐 권한 구조 자체의 불편함 — "부관리자에게 모든 권한을 줄 수 없다"

> ~~**#12 - 총관리자가 위임할 수 있는 권한이 8개 중 4개뿐, 나머지 3개는 위임 자체가 불가능**~~ — `User`에 `canManageUsers`(계정 관리)/`canManageAdminPermissions`(관리자 권한 부여)/`canViewAuditLog`(감사 로그) 3개 플래그 신규 추가, `AdminAccessInterceptor`의 하드코딩된 `throw`를 다른 4개 권한과 동일하게 플래그 체크로 교체했다. `/admin/users/**` 안에서도 역할/권한 자체를 바꾸는 promote·demote·permissions·admins 화면은 `canManageAdminPermissions`로, 계정 목록/정지/탈퇴 같은 나머지는 `canManageUsers`로 더 세밀하게 나눴다(둘을 분리한 이유: "계정 정지 가능"과 "다른 계정에 관리자 권한을 몰아줄 수 있음"은 위험도가 다른 별개 권한이라서). 문서가 우려했던 권한 상승 경로(부관리자가 자기 자신에게 권한을 추가로 켜는 것)는 `AdminUserService.updatePermissions()`에 `setRole()`/`deleteUser()`와 동일한 자기 자신 체크를 추가해 서버 단에서 차단 — 실제로 `canManageAdminPermissions`를 가진 부관리자 계정으로 자기 자신을 대상으로 직접 POST를 보내 "본인의 권한은 스스로 변경할 수 없습니다." 거부를 확인했고, `admin-permissions.html`도 자기 자신 행에는 아예 토글 폼 대신 안내 문구만 보이도록 숨겼다(계정 관리 화면의 승격/강등 버튼도 `canManageAdminPermissions` 없이 `canManageUsers`만 있는 계정에는 안 보이도록 같이 정리 - 눌러도 항상 거부되는 버튼이 보이던 부작용을 발견해서 함께 고침). **해결 확인, 삭제.**

> ~~**#13 - 총관리자는 오직 1명만 존재할 수 있고, 그 계정을 앱 안에서 늘리거나 넘겨줄 방법이 없음**~~ — `AdminUserService.promoteToSuperAdmin()` 신규 + 관리자 권한 관리 화면에 "총관리자로 승격" 버튼 추가. `ROLE_SUPER_ADMIN`을 더 이상 단일 계정으로 제한하지 않고 여러 명 존재 가능하도록 설계를 바꿨다 - `AdminAccessInterceptor`가 `canManageAdminPermissions` 플래그로도 관리자 권한 화면 접근을 허용하게 됐으므로, "총관리자를 새로 만드는" 이 액션만큼은 인터셉터를 믿지 않고 서비스 단에서 `actingAdmin.isSuperAdmin()`을 별도로 재확인한다(권한 부여 화면에 들어올 수 있다고 해서 총관리자를 늘릴 수 있는 건 아니어야 함). 실제 총관리자가 아닌 `canManageAdminPermissions` 보유 부관리자로 직접 POST를 보내 "총관리자만 다른 계정을 총관리자로 승격할 수 있습니다." 거부를 확인했고, 진짜 총관리자로는 정상 승격되어 두 번째 총관리자 계정이 만들어지는 것까지 브라우저로 end-to-end 확인. **해결 확인, 삭제.**

> ~~**권한을 전부 회수당한 부관리자가 여전히 "부관리자" 신분으로만 표시됨**~~ — 두 옵션(`Role` 자동 전환 vs 배지 표시) 중 리스크가 낮은 쪽으로 처리: `Role`은 그대로 두고(CLAUDE.md에 기록된 "마지막 관리자 강등 방지 가드" 전제를 흔들 수 있어 회피), `admin-permissions.html`이 이미 쓰는 것과 동일한 조건으로 마이페이지 "부관리자" 배지 옆에도 "권한 없음" 배지를 추가했다. 권한 0개인 `user1`로 로그인해 마이페이지에 "부관리자 권한 없음"이 뜨는 것 확인. **해결 확인, 삭제.**

---

## 📱 모바일 환경(375px 기준)
- ✅ 상단 내비게이션 햄버거 메뉴, 커뮤니티 목록, 관리자 표 가로 스크롤 — 문제 없음.
- ✅ 관리자 사이드 탭이 세로로 안 늘어나고 가로 스크롤로 처리됨(`overflow-x: auto`가 자기 자신에만 걸려있어서 페이지 전체는 안 밀림).
- ✅ (해결됨) 감사 로그/신고 관리 검색창이 페이지 전체를 밀던 문제 — `document.body.scrollWidth`가 375(뷰포트와 동일)로 확인, 위 "관리자" 섹션 참고.
- ✅ (해결됨) 모바일 관리자 탭바 스크롤 힌트 — 위 "관리자" 섹션 참고.
- ✅ (해결됨) 6번(아이디 찾기 없음) — 위 "일반 사용자" 섹션 참고.

---

## 우선순위 제안

**체감 영향이 큰 순서**
1. ~~권한 구조 — 계정 관리/관리자 권한/감사 로그 위임 불가(#12), 총관리자 단일 장애점(#13)~~ — 2026-08-25 완료(위 "권한 구조 자체의 불편함" 섹션 참고).
2. ~~아이디/비밀번호 찾기(#3, #6)~~ — 2026-08-24 완료(위 "백엔드"/"일반 사용자" 섹션 참고).

**이 문서에 남은 실질적 항목 없음** — 위 두 라운드로 4차 정리에서 발견된 모든 버그/구조적 문제가 해결됐다. 새로 발견되는 문제는 다음 라운드 조사에서 새 문서로 정리할 것.

---

## 디자인

새 디자인 시스템으로 다시 짜는 큰 방향은 여전히 [설계도 아티팩트](https://claude.ai/code/artifact/7ff275a2-1e10-4ccc-b659-28ec6a39e242)에 정리돼 있지만, 거기 지적됐던 구체적인 버그 3개는 **이번 라운드에 실제 코드로 수정하고 브라우저로 재검증까지 마쳤다** (총관리자로 로그인해 `/admin/audit-log`, `/admin/users/{id}/profile`을 직접 확인).

1. **관리자 통계 박스(`.admin-profile-stat`, 계정 프로필의 "작성 게시글/댓글" 박스) 대비 부족** — 이 박스는 라이트/다크 모드와 무관하게 배경이 항상 `--nav-bg`(짙은 남색, `#17153a`)로 고정인데, 안의 글자색은 라이트/다크에 따라 밝기가 바뀌는 `--brand-1`/`--muted`를 그대로 쓰고 있었다. 특히 라이트 모드의 `--muted`(`#6b7280`)는 이 어두운 고정 배경 위에서 WCAG AA 기준(4.5:1)을 못 채웠다. `theme.css`의 화면별 토큰 대신, 이 배경 전용 고정 밝은 톤(`#a5b4fc`/`#c7cad9`)으로 분리해서 라이트·다크 어느 모드에서도 10:1 이상의 대비가 나오도록 고쳤다([admin.css](src/main/resources/static/css/admin.css)).
2. **날짜 필터(`<input type="date">`) 다크모드 미대응** — 배경/글자색은 이미 `theme.css`의 다크모드 규칙을 타고 있었지만, 브라우저가 그리는 달력 아이콘 같은 네이티브 폼 컨트롤 크롬은 `color-scheme`을 별도로 안 알려주면 항상 라이트 테마(검은 아이콘)로 그려져서 어두운 입력창 배경 위에서 거의 안 보였다. 다크모드 입력창 규칙에 `color-scheme: dark`를 추가해서 해결([theme.css](src/main/resources/static/css/theme.css)).
3. **감사 로그 색상 배지 불일치** — 템플릿이 "액션이 BLIND/DELETE/DEACTIVATE/DEMOTE면 빨강, 나머지는 전부 초록"이라는 이분법으로 배지 색을 정하고 있어서, `REPORT_UNCLEAR`("문제없음 철회" — 다시 문제 있다고 되돌리는 조치인데 초록으로 표시됨)와 `PERMISSIONS`("권한 변경" — 긍정도 부정도 아닌데 초록으로 표시됨)가 실제 의미와 반대/부정확하게 칠해지고 있었다. 액션별 배지 톤을 서버(`AdminActionLogService`)에서 명시적으로 매핑하도록 바꾸고, 어느 쪽도 아닌 액션을 위한 중립 회색 배지(`admin-status-neutral`)를 새로 추가했다 — 앞으로 새 액션 타입을 추가해도 분류가 누락되면 중립 회색으로 폴백해서 조용히 잘못된 색으로 새지 않는다.

검증: 실제 감사 로그 데이터(블라인드/블라인드 해제/삭제/승격/강등)로 배지 클래스가 의미에 맞게 나오는 것, 통계 박스 색상이 라이트 모드에서도 충분한 대비로 렌더링되는 것, 날짜 입력창이 다크모드에서 `color-scheme: dark`를 상속받는 것까지 브라우저 콘솔로 직접 확인했다.
